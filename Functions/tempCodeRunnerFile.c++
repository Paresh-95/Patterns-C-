#include<iostream>
#include <cmath>
using namespace std;

float areaOfCirlce()
{
    float area = pii*(2*radius);
    return area;
}
;
int main(){

    int radius
    cout << "Enter radius = ";
    cin >> radius;
    cout << radius;
    float pi = M_PI;
    cout << pi;
    cout << "Area of Circle is"+areaOfCirlce(pi,radius);
}